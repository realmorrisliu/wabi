---
description: Delegate implementation to the worker agent
argument-hint: "<task description>"
---
Use the subagent tool in single-agent mode with the `worker` agent to implement:

$@

Report what changed and what was verified.
