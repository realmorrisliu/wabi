---
description: Ask the read-only planner agent for an implementation plan (no implementation)
argument-hint: "<task>"
---
Delegate planning for this task to the `planner` agent with the subagent tool:

$@

Run it in the foreground (or background for non-blocking planning) and report the resulting plan. The planner is read-only and must not implement anything; implementation stays with you.
