---
description: Delegate coding work to the worker agent
argument-hint: "[--background] <task>"
---
Delegate this task to the `worker` agent with the subagent tool:

$@

Treat an optional leading `--background` as the subagent `background` flag and exclude it from the task. Otherwise run in the foreground. Report the worker's result and verification.
