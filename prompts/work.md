---
description: Delegate coding work to the worker agent
argument-hint: "<task>"
---
Delegate this task to the `worker` agent with the subagent tool:

$@

Run it in the foreground and report the worker's result and verification. Worker is write-capable and cannot run in the background.
